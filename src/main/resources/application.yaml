spring:
  application:
    name: SpringCRUD

  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_neutron_db?serverTimezone=Asia/Kolkata
    username: neutronUser
    password: 4321

  jpa:
    hibernate:
      ddl-auto: update
      ddl-show: true

server:
  port: 8080
